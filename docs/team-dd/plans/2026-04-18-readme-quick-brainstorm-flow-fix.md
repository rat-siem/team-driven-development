# README Quick Brainstorm Flow Fix Implementation Plan

> **For agentic workers:** Use team-driven-development to execute this plan.

**Goal:** Fix the Quick Brainstorm flow notation in `README.md` and `docs/README.ja.md` to include `team-plan` and exclude `sprint-master` (an internal subagent), and rewrite the Usage description so it matches the current implementation.
**Architecture:** Two sequential documentation tasks. Each task edits both the English and Japanese README at one conceptual location (Task 1: Key Features entry, Task 2: Usage subsection) using exact `old_string → new_string` Edit calls, verifies with `grep`, and commits.
**Tech Stack:** Markdown.
**Spec:** docs/team-dd/specs/2026-04-18-readme-quick-brainstorm-flow-fix-design.md (authoritative; consult for rationale/decisions)
**Sprints:** sprints/2026-04-18-readme-quick-brainstorm-flow-fix/ (Sprint Contract files, generated by sprint-master)

---

## File Structure

| File | Status | Responsibility |
| --- | --- | --- |
| `README.md` | Modify | Key Features Quick Brainstorm entry (Task 1); Usage "With Quick Brainstorm (self-contained)" subsection (Task 2). |
| `docs/README.ja.md` | Modify | Mirror each English edit in Japanese, same section order (Tasks 1–2). |

---

### Task 1: Key Features — fix Quick Brainstorm flow chain (drop `sprint-master`)

**Files:**
- Modify: `README.md`
- Modify: `docs/README.ja.md`

**Spec ref:** docs/team-dd/specs/2026-04-18-readme-quick-brainstorm-flow-fix-design.md#section-by-section-changes

- [ ] Step 1: Confirm the pre-edit state of both READMEs
  Run: `grep -q "quick-brainstorm → team-plan → sprint-master → team-driven-development" README.md && grep -q "quick-brainstorm → team-plan → sprint-master → team-driven-development" docs/README.ja.md`
  Expected: exit code 0 (both files still have the outdated chain)

- [ ] Step 2: Replace the Key Features Quick Brainstorm entry in `README.md`

  Use `Edit` with `file_path=README.md`.

  old_string:
  ```
  - **Quick Brainstorm** — Lightweight spec + plan generation with minimal dialogue. Infers what it can from context, asks only what's genuinely ambiguous, and outputs full-quality documents. Hands off via `quick-brainstorm → team-plan → sprint-master → team-driven-development`. Use `/quick-brainstorm` or let team-driven-development suggest it when no plan exists.
  ```

  new_string:
  ```
  - **Quick Brainstorm** — Lightweight spec + plan generation with minimal dialogue. Infers what it can from context, asks only what's genuinely ambiguous, and outputs full-quality documents. Hands off via `quick-brainstorm → team-plan → team-driven-development` (`team-plan` invokes `sprint-master` internally to generate Sprint Contract files). Use `/quick-brainstorm` or let team-driven-development suggest it when no plan exists.
  ```

- [ ] Step 3: Replace the Key Features Quick Brainstorm entry in `docs/README.ja.md`

  Use `Edit` with `file_path=docs/README.ja.md`.

  old_string:
  ```
  - **Quick Brainstorm** — 最小限の対話で本格的な spec + plan を生成する軽量スキル。コンテキストから推論できることは推論し、本当に曖昧な点のみ質問する。引き渡しは `quick-brainstorm → team-plan → sprint-master → team-driven-development`。`/quick-brainstorm` で呼び出すか、plan なしで team-driven-development を呼ぶと自動提案される。
  ```

  new_string:
  ```
  - **Quick Brainstorm** — 最小限の対話で本格的な spec + plan を生成する軽量スキル。コンテキストから推論できることは推論し、本当に曖昧な点のみ質問する。引き渡しは `quick-brainstorm → team-plan → team-driven-development`（`team-plan` が内部で `sprint-master` を呼び出して Sprint Contract ファイルを生成する）。`/quick-brainstorm` で呼び出すか、plan なしで team-driven-development を呼ぶと自動提案される。
  ```

- [ ] Step 4: Verify the chain is corrected and the old chain is gone
  Run: `! grep -q "quick-brainstorm → team-plan → sprint-master → team-driven-development" README.md && ! grep -q "quick-brainstorm → team-plan → sprint-master → team-driven-development" docs/README.ja.md && grep -q "quick-brainstorm → team-plan → team-driven-development" README.md && grep -q "quick-brainstorm → team-plan → team-driven-development" docs/README.ja.md`
  Expected: exit code 0 (old chain absent in both files; new chain present in both files)

- [ ] Step 5: Commit
  ```bash
  git add README.md docs/README.ja.md
  git commit -m "docs(readme): drop sprint-master from Key Features Quick Brainstorm flow chain"
  ```

---

### Task 2: Usage — add `team-plan` to Quick Brainstorm flow and rewrite description

**Files:**
- Modify: `README.md`
- Modify: `docs/README.ja.md`

**Spec ref:** docs/team-dd/specs/2026-04-18-readme-quick-brainstorm-flow-fix-design.md#section-by-section-changes

- [ ] Step 1: Confirm the pre-edit state of both READMEs
  Run: `grep -q "/quick-brainstorm <task description> → team-driven-development" README.md && grep -q "/quick-brainstorm <タスクの説明> → team-driven-development" docs/README.ja.md`
  Expected: exit code 0 (Usage subsection in both files still has the outdated chain)

- [ ] Step 2: Replace the Usage Quick Brainstorm subsection body in `README.md`

  Use `Edit` with `file_path=README.md`.

  old_string:
  ````
  ### With Quick Brainstorm (self-contained)

  ```
  /quick-brainstorm <task description> → team-driven-development
  ```

  The `quick-brainstorm` skill generates a spec and plan with minimal dialogue. When the plan is ready, it offers to hand off directly to team-driven-development for execution. If team-driven-development is invoked without a plan, it will suggest quick-brainstorm automatically.
  ````

  new_string:
  ````
  ### With Quick Brainstorm (self-contained)

  ```
  /quick-brainstorm <task description> → team-plan → team-driven-development
  ```

  The `quick-brainstorm` skill generates a spec with minimal dialogue. When the spec is approved, it hands off to `team-plan`, which produces the implementation plan and invokes `sprint-master` internally to generate Sprint Contract files. When the plan is ready, the flow offers to hand off to team-driven-development for execution. If team-driven-development is invoked without a plan, it will suggest quick-brainstorm automatically.
  ````

- [ ] Step 3: Replace the Usage Quick Brainstorm subsection body in `docs/README.ja.md`

  Use `Edit` with `file_path=docs/README.ja.md`.

  old_string:
  ````
  ### Quick Brainstorm と併用（自己完結）

  ```
  /quick-brainstorm <タスクの説明> → team-driven-development
  ```

  `quick-brainstorm` スキルは最小限の対話で spec と plan を生成します。plan が完成すると team-driven-development への引き渡しを提案します。plan なしで team-driven-development を呼び出した場合は、自動的に quick-brainstorm を提案します。
  ````

  new_string:
  ````
  ### Quick Brainstorm と併用（自己完結）

  ```
  /quick-brainstorm <タスクの説明> → team-plan → team-driven-development
  ```

  `quick-brainstorm` スキルは最小限の対話で spec を生成します。spec が承認されると `team-plan` に引き渡され、`team-plan` が plan を生成し、内部で `sprint-master` を呼び出して Sprint Contract ファイルを生成します。plan が完成すると team-driven-development への引き渡しを提案します。plan なしで team-driven-development を呼び出した場合は、自動的に quick-brainstorm を提案します。
  ````

- [ ] Step 4: Verify the new chain and description are present and the old wording is gone
  Run: `! grep -q "/quick-brainstorm <task description> → team-driven-development" README.md && ! grep -q "/quick-brainstorm <タスクの説明> → team-driven-development" docs/README.ja.md && grep -q "/quick-brainstorm <task description> → team-plan → team-driven-development" README.md && grep -q "/quick-brainstorm <タスクの説明> → team-plan → team-driven-development" docs/README.ja.md && ! grep -q "generates a spec and plan with minimal dialogue" README.md && ! grep -q "spec と plan を生成します" docs/README.ja.md`
  Expected: exit code 0 (old chain and old description absent in both files; new chain present in both)

- [ ] Step 5: Convention sanity — confirm `sprint-master` no longer appears in any arrow chain across both READMEs
  Run: `! grep -E "sprint-master *(→|->)" README.md && ! grep -E "(→|->) *sprint-master" README.md && ! grep -E "sprint-master *(→|->)" docs/README.ja.md && ! grep -E "(→|->) *sprint-master" docs/README.ja.md`
  Expected: exit code 0 (no occurrences of `sprint-master` adjacent to an arrow in either file)

- [ ] Step 6: Commit
  ```bash
  git add README.md docs/README.ja.md
  git commit -m "docs(readme): add team-plan to Usage Quick Brainstorm flow and rewrite description"
  ```
