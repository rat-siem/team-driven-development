# Rename `quick-plan` → `quick-brainstorm` Implementation Plan

> **For agentic workers:** Use team-driven-development to execute this plan.

**Goal:** Rename the `quick-plan` skill to `quick-brainstorm` across the repository with zero behavior change.
**Architecture:** Two sequential tasks — (1) `git mv` the skill directory and patch its own self-references; (2) replace `quick-plan` / `Quick Plan` across seven external reference files. Verification is textual grep-based; no runtime component.
**Tech Stack:** Markdown, shell (`git`, `grep`).
**Spec:** ../specs/2026-04-18-quick-brainstorm-rename-design.md (authoritative; consult for rationale/decisions)
**Sprints:** sprints/quick-brainstorm-rename/ (Sprint Contract files, generated by sprint-master)

---

## File Structure

| File | Status | Responsibility |
| --- | --- | --- |
| `skills/quick-plan/` | Remove (via `git mv`) | Source directory — relocated to `skills/quick-brainstorm/` |
| `skills/quick-brainstorm/SKILL.md` | Modify (post-`git mv`) | Self-references: frontmatter `name`, H1, intro prose, announce string, digraph identifier |
| `skills/team-plan/SKILL.md` | Modify | Replace `quick-plan` reference in Invocation section (line 63) |
| `skills/team-driven-development/SKILL.md` | Modify | Replace `quick-plan` on lines 14 and 22 (two occurrences) |
| `skills/solo-review/SKILL.md` | Modify | Replace both `quick-plan` occurrences on line 167 |
| `skills/deep-brainstorm/SKILL.md` | Modify | Replace `quick-plan` on lines 270 and 278 |
| `README.md` | Modify | Replace all `quick-plan` / `Quick Plan` occurrences (lines 59, 155, 158, 161, 250) |
| `docs/README.ja.md` | Modify | Replace all occurrences mirroring `README.md` (lines 58, 154, 157, 160, 249) |
| `guidelines/writing.md` | Modify | Replace `quick-plan` on lines 84 and 85 |

---

### Task 1: Rename skill directory and update self-references

**Files:**
- Move: `skills/quick-plan/` → `skills/quick-brainstorm/` (via `git mv`)
- Modify: `skills/quick-brainstorm/SKILL.md`

**Spec ref:** docs/team-dd/specs/2026-04-18-quick-brainstorm-rename-design.md#directory-move

- [ ] Step 1: Write the failing verification
  ```bash
  # Post-state assertions: old dir gone, new dir present, SKILL.md self-refs updated,
  # and no lingering old-name tokens inside the file.
  test ! -e skills/quick-plan \
    && test -f skills/quick-brainstorm/SKILL.md \
    && grep -q '^name: quick-brainstorm$' skills/quick-brainstorm/SKILL.md \
    && grep -q '^# Quick Brainstorm$' skills/quick-brainstorm/SKILL.md \
    && grep -qF "I'm using quick-brainstorm to generate a spec and hand off to team-plan." skills/quick-brainstorm/SKILL.md \
    && grep -q '^digraph quick_brainstorm {$' skills/quick-brainstorm/SKILL.md \
    && ! grep -qE 'quick-plan|Quick Plan|quick_plan' skills/quick-brainstorm/SKILL.md
  ```
- [ ] Step 2: Run verification to confirm it fails
  Run: `bash -c 'test ! -e skills/quick-plan && test -f skills/quick-brainstorm/SKILL.md && grep -q "^name: quick-brainstorm$" skills/quick-brainstorm/SKILL.md && grep -q "^# Quick Brainstorm$" skills/quick-brainstorm/SKILL.md && grep -qF "I'"'"'m using quick-brainstorm to generate a spec and hand off to team-plan." skills/quick-brainstorm/SKILL.md && grep -q "^digraph quick_brainstorm {$" skills/quick-brainstorm/SKILL.md && ! grep -qE "quick-plan|Quick Plan|quick_plan" skills/quick-brainstorm/SKILL.md'; echo "exit=$?"`
  Expected: FAIL (`exit=1` or higher — `skills/quick-plan` still exists; `skills/quick-brainstorm/SKILL.md` does not yet).
- [ ] Step 3: Rename the directory and patch self-references
  ```bash
  git mv skills/quick-plan skills/quick-brainstorm
  ```
  Then apply the following exact text replacements inside `skills/quick-brainstorm/SKILL.md` using the Edit tool (each `old_string` is unique in the file):

  - `name: quick-plan` → `name: quick-brainstorm`
  - `# Quick Plan` → `# Quick Brainstorm`
  - `quick-plan infers what it can from context` → `quick-brainstorm infers what it can from context`
  - `"I'm using quick-plan to generate a spec and hand off to team-plan."` → `"I'm using quick-brainstorm to generate a spec and hand off to team-plan."`
  - `digraph quick_plan {` → `digraph quick_brainstorm {`

  Do NOT modify the `description:` line (no `quick-plan` substring present; spec keeps it unchanged).
- [ ] Step 4: Run verification to confirm it passes
  Run: `bash -c 'test ! -e skills/quick-plan && test -f skills/quick-brainstorm/SKILL.md && grep -q "^name: quick-brainstorm$" skills/quick-brainstorm/SKILL.md && grep -q "^# Quick Brainstorm$" skills/quick-brainstorm/SKILL.md && grep -qF "I'"'"'m using quick-brainstorm to generate a spec and hand off to team-plan." skills/quick-brainstorm/SKILL.md && grep -q "^digraph quick_brainstorm {$" skills/quick-brainstorm/SKILL.md && ! grep -qE "quick-plan|Quick Plan|quick_plan" skills/quick-brainstorm/SKILL.md'; echo "exit=$?"`
  Expected: PASS (`exit=0`).
- [ ] Step 5: Commit
  ```bash
  git add skills/quick-brainstorm/SKILL.md
  git commit -m "refactor(quick-brainstorm): rename skill directory from quick-plan

  Move skills/quick-plan/ to skills/quick-brainstorm/ via git mv and update
  the SKILL.md self-references (frontmatter name, H1, intro prose, announce
  string, digraph identifier). Behavior unchanged."
  ```

---

### Task 2: Update external `quick-plan` references

**Files:**
- Modify: `skills/team-plan/SKILL.md`
- Modify: `skills/team-driven-development/SKILL.md`
- Modify: `skills/solo-review/SKILL.md`
- Modify: `skills/deep-brainstorm/SKILL.md`
- Modify: `README.md`
- Modify: `docs/README.ja.md`
- Modify: `guidelines/writing.md`

**Spec ref:** docs/team-dd/specs/2026-04-18-quick-brainstorm-rename-design.md#other-in-repo-reference-updates

- [ ] Step 1: Write the failing verification
  ```bash
  # Two-part check:
  # (a) No `quick-plan` / `quick_plan` / `Quick Plan` tokens anywhere in the repo
  #     outside .git and docs/ (spec-authoritative verification).
  # (b) docs/README.ja.md must also be clean — the broad grep excludes docs/ by
  #     design, so we assert this file separately.
  # (c) Every expected file contains `quick-brainstorm`.
  ! grep -rnE 'quick-plan|quick_plan|Quick Plan' . --exclude-dir=.git --exclude-dir=docs \
    && ! grep -nE 'quick-plan|quick_plan|Quick Plan' docs/README.ja.md \
    && [ -z "$(grep -L 'quick-brainstorm' \
         skills/quick-brainstorm/SKILL.md \
         skills/team-plan/SKILL.md \
         skills/team-driven-development/SKILL.md \
         skills/solo-review/SKILL.md \
         skills/deep-brainstorm/SKILL.md \
         README.md \
         docs/README.ja.md \
         guidelines/writing.md)" ]
  ```
- [ ] Step 2: Run verification to confirm it fails
  Run: `bash -c '! grep -rnE "quick-plan|quick_plan|Quick Plan" . --exclude-dir=.git --exclude-dir=docs && ! grep -nE "quick-plan|quick_plan|Quick Plan" docs/README.ja.md && [ -z "$(grep -L quick-brainstorm skills/quick-brainstorm/SKILL.md skills/team-plan/SKILL.md skills/team-driven-development/SKILL.md skills/solo-review/SKILL.md skills/deep-brainstorm/SKILL.md README.md docs/README.ja.md guidelines/writing.md)" ]'; echo "exit=$?"`
  Expected: FAIL (`exit=1`) — the seven external files still reference `quick-plan` and some do not yet contain `quick-brainstorm`.
- [ ] Step 3: Apply the external replacements
  Using the Edit tool, perform the replacements below. Each `old_string` was verified unique in its file via grep context; if a match becomes ambiguous, include a larger surrounding snippet.

  **`skills/team-plan/SKILL.md` (line 63):**
  - `` handoff from `deep-brainstorm` or `quick-plan`. `` → `` handoff from `deep-brainstorm` or `quick-brainstorm`. ``

  **`skills/team-driven-development/SKILL.md` (lines 14 and 22):**
  - `` suggest the `quick-plan` skill first `` → `` suggest the `quick-brainstorm` skill first ``
  - `` Suggest `quick-plan` skill. If accepted, invoke with `` → `` Suggest `quick-brainstorm` skill. If accepted, invoke with ``

  **`skills/solo-review/SKILL.md` (line 167, both occurrences on one line):** apply via `replace_all: true` since the literal `quick-plan` appears exactly twice and both should change:
  - `quick-plan` → `quick-brainstorm` (`replace_all: true` — verify exactly 2 replacements on line 167)

  **`skills/deep-brainstorm/SKILL.md` (lines 270 and 278):**
  - `` "either is fine"/"up to you") — mirror `quick-plan`: pick the most comprehensive `` → `` "either is fine"/"up to you") — mirror `quick-brainstorm`: pick the most comprehensive ``
  - `` **Coexists with**: `quick-plan` (requirements already clear). `` → `` **Coexists with**: `quick-brainstorm` (requirements already clear). ``

  **`README.md` (lines 59, 155, 158, 161, 250):**
  - `- **Quick Plan** — Lightweight spec + plan generation` → `- **Quick Brainstorm** — Lightweight spec + plan generation`
  - `Use \`/quick-plan\` or let team-driven-development suggest it` → `Use \`/quick-brainstorm\` or let team-driven-development suggest it`
  - `### With Quick Plan (self-contained)` → `### With Quick Brainstorm (self-contained)`
  - `/quick-plan <task description> → team-driven-development` → `/quick-brainstorm <task description> → team-driven-development`
  - `The \`quick-plan\` skill generates a spec and plan with minimal dialogue` → `The \`quick-brainstorm\` skill generates a spec and plan with minimal dialogue`
  - `it will suggest quick-plan automatically.` → `it will suggest quick-brainstorm automatically.`
  - `When a user defers a decision during quick-plan's clarification phase` → `When a user defers a decision during quick-brainstorm's clarification phase`

  **`docs/README.ja.md` (lines 58, 154, 157, 160, 249):**
  - `- **Quick Plan** — 最小限の対話で本格的な spec + plan を生成する軽量スキル` → `- **Quick Brainstorm** — 最小限の対話で本格的な spec + plan を生成する軽量スキル`
  - `\`/quick-plan\` で呼び出すか、plan なしで team-driven-development を呼ぶと自動提案される` → `\`/quick-brainstorm\` で呼び出すか、plan なしで team-driven-development を呼ぶと自動提案される`
  - `### Quick Plan と併用（自己完結）` → `### Quick Brainstorm と併用（自己完結）`
  - `/quick-plan <タスクの説明> → team-driven-development` → `/quick-brainstorm <タスクの説明> → team-driven-development`
  - `\`quick-plan\` スキルは最小限の対話で spec と plan を生成します` → `\`quick-brainstorm\` スキルは最小限の対話で spec と plan を生成します`
  - `自動的に quick-plan を提案します。` → `自動的に quick-brainstorm を提案します。`
  - `quick-plan の質問フェーズでユーザーが判断を委任した場合` → `quick-brainstorm の質問フェーズでユーザーが判断を委任した場合`

  **`guidelines/writing.md` (lines 84 and 85):**
  - `` invoked directly or via `quick-plan` / `deep-brainstorm` `` → `` invoked directly or via `quick-brainstorm` / `deep-brainstorm` ``
  - `` output of `brainstorming` / `deep-brainstorm` / `quick-plan`. `` → `` output of `brainstorming` / `deep-brainstorm` / `quick-brainstorm`. ``
- [ ] Step 4: Run verification to confirm it passes
  Run: `bash -c '! grep -rnE "quick-plan|quick_plan|Quick Plan" . --exclude-dir=.git --exclude-dir=docs && ! grep -nE "quick-plan|quick_plan|Quick Plan" docs/README.ja.md && [ -z "$(grep -L quick-brainstorm skills/quick-brainstorm/SKILL.md skills/team-plan/SKILL.md skills/team-driven-development/SKILL.md skills/solo-review/SKILL.md skills/deep-brainstorm/SKILL.md README.md docs/README.ja.md guidelines/writing.md)" ]'; echo "exit=$?"`
  Expected: PASS (`exit=0`).
- [ ] Step 5: Commit
  ```bash
  git add skills/team-plan/SKILL.md \
          skills/team-driven-development/SKILL.md \
          skills/solo-review/SKILL.md \
          skills/deep-brainstorm/SKILL.md \
          README.md \
          docs/README.ja.md \
          guidelines/writing.md
  git commit -m "docs(quick-brainstorm): update references to renamed skill

  Replace every remaining quick-plan / Quick Plan reference across skills,
  README, README.ja, and guidelines. Historical docs/ artifacts are left
  unchanged per spec."
  ```
