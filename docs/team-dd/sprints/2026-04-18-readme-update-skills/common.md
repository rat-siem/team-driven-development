# Sprint Contract: README Update for deep-brainstorm / team-plan / sprint-master

## Spec
docs/team-dd/specs/2026-04-18-readme-update-skills-design.md

## Plan
docs/team-dd/plans/2026-04-18-readme-update-skills.md

## Shared Criteria
- Only `README.md` and `docs/README.ja.md` may be edited. No changes to skill files, `CLAUDE.md`, `.claude-plugin/*.json`, `agents/**`, `templates/**`, `scripts/**`, `sprints/**`, or `guidelines/**`.
- English and Japanese READMEs stay structurally parallel: same section order, same section headings (translated), same bullet count per Key Features block and per Phase.
- All edits preserve existing Markdown tone: declarative English in `README.md`, です・ます form in `docs/README.ja.md` with technical terms (Sprint Contract, sprint-master, worktree, etc.) left in English.
- Each task produces exactly one commit following the repo's `type(scope): subject` convention (see recent history: `docs(readme): ...`, `refactor(quick-brainstorm): ...`, `docs(quick-brainstorm): ...`). Commit touches only the two README files.
- Bullets and headings use display labels that follow the existing convention: title case for display (`Quick Brainstorm`, `Deep Brainstorm`, `Team Plan`, `Sprint Master`, `Solo Review`), and lowercase hyphenated form when referring to the skill by its invocation name or in handoff chains (`quick-brainstorm → team-plan → sprint-master → team-driven-development`).
- No edits outside the files enumerated in the plan's `## File Structure` table.
- Superpowers is removed from the README narrative: no remaining mention of `superpowers`, `Superpowers`, `[Superpowers]`, or `github.com/obra/superpowers` in either README after all three tasks land.

## Domain Guidelines
- writing: guidelines/writing.md
